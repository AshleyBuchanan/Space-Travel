import '../css/FormComponent.css';

const DetailsFormComponent = ({children}) => {
    return (
        <div className="form-component">
            {children}
        </div>
    );
};

export default DetailsFormComponent;
